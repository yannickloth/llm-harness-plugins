--- name: migrate-from-claude-code description: Full migration pipeline from Claude Code to OpenCode — discovers all agent sources (project, global, plugins, submodules), converts tools to permission format, fixes skill paths, improves descriptions, adds compatibility fields, rewrites opencode.json, sets agent modes, converts CLAUDE.md to AGENTS.md, and verifies with an iterative boot-test-fix cycle. Use when porting a Claude-Code-configured project to OpenCode. compatibility: Requires opencode, git, python3 --- # Claude Code → OpenCode Migration Procedure **Source**: `ivp-book-series` migration sessions (`ses_03aac885...`, `ses_0392b1d8...`, etc.) and commits `8db2e3b1`, `410e977c`, `b3d80471`, `72079881` (Aug 3, 2026). **Pipeline**: 10 phases, sequential, each with preconditions, postconditions, and verification gates. --- ## Phase 0: Discover the agent landscape **Precondition**: `.claude/` directory exists with agents, skills, context files. `opencode.json` may or may not exist at project root. **Postcondition**: Complete catalog of all agent sources, duplicates identified, dead plugins removed, non-agent files audited. Migration surface area is fully mapped. **Gates**: None — this is discovery only. If `.claude/` is absent, abort (already migrated). --- ### 0.1 Catalog every agent source OpenCode discovers agents from: (a) `.opencode/agents/*.md`, (b) `opencode.json` `agent` section, (c) `~/.config/opencode/agents/*.md`. Claude Code uses: `.claude/agents/*.md`, plugins, `~/.claude/agents/*.md`. ```bash # Project agents find .claude/agents/ -name '*.md' ! -name 'README.md' | sort # Global agents find ~/.claude/agents/ -name '*.md' 2>/dev/null | sort # opencode.json agent count python3 -c "import json; d=json.load(open('opencode.json')); print(len(d.get('agent',{})))" 2>/dev/null # Plugin agents (if llm-harness-plugins exists) find ../llm-harness-plugins/ -path '*/agents/*.md' 2>/dev/null | sort ``` **Expected result**: counts for each source. In ivp-book: 182 project agents, 15 global, 186 in opencode.json, 30 plugin agents. ### 0.2 Identify duplicates across agent sources **Hypothesis**: agents defined in BOTH `.claude/agents/` and a plugin directory are redundant. **Decision**: keep only the plugin copy; remove the `.claude/agents/` duplicate. The plugin `.md` serves as the shared prompt body; the `opencode.json` `agent` block provides model/permission config. In ivp-book: 17 duplicates: `citation-fidelity-auditor`, `logic-auditor`, `math-verifier`, `proof-soundness-auditor`, `redundancy-auditor`, `style-auditor`, `style-naturalizer`, `xref-checker`, `typst-citation-checker`, `typst-diagram-checker`, `typst-formatting-fixer`, `typst-production-readiness-checker`, `typst-syntax-fixer`, `typst-xref-checker`, `haiku-general`, `sonnet-general`, `opus-general`. ```bash # Find .claude/agents/*.md that also exist in any plugin for f in .claude/agents/*.md; do name=$(basename "$f" .md) plugin_path=$(find ../llm-harness-plugins/ -name "${name}.md" 2>/dev/null | head -1) [ -n "$plugin_path" ] && echo "DUPLICATE: $name — .claude/ copy + plugin at $plugin_path" done ``` **Expected result**: list of duplicate names (may be empty if no plugins share agents). ### 0.3 Discover plugin-agent `.md` files **Hypothesis**: plugin agent `.md` files are NOT auto-discovered by OpenCode. They must be registered explicitly. Two registration paths: | Path | Mechanics | When to use | |------|-----------|-------------| | **A** — Copy to `.opencode/agents/` | Frontmatter + body auto-discovered; no `opencode.json` entry needed | Duplicates the file | | **B** — `opencode.json` entry with `prompt:` | Body loads as system prompt; **frontmatter is ignored**; must restate model/mode/permissions in JSON | Single source of truth; ivp-book default | **Plugin skills**: Skills in plugin directories are NOT auto-discovered either. OpenCode only scans `.opencode/skills/`, `.claude/skills/`, `.agents/skills/`, and `~/.config/` equivalents. To use a plugin skill, add an entry in `opencode.json`: ```json "skill": { "my-skill": "prompt: {file:../llm-harness-plugins/my-plugin/skills/my-skill/SKILL.md}" } ``` ### 0.4 Inventory all `.claude/` files ```bash find .claude/ -type f | sort ``` **Expected categories beyond agents/skills:** | File/dir | Disposition | |---|---| | `.claude/CLAUDE.md` | → `AGENTS.md` (Phase 8) | | `.claude/settings.json` | Delete — replaced by agent-level `permission:` | | `.claude/hooks/` | Delete — no opencode equivalent; port to plugins or drop | | `.claude/memory/` | Read → extract learnings to `AGENTS.md` prose; then delete | | `.claude/projects/` | Same as memory | | `.claude/REVIEW_SYSTEM.md` | Delete — skills are self-documenting | | `.claudelens` | Delete — no opencode equivalent | | Loose `.md` files | Move to `.agents/context/` (Phase 1) | | Subdir `.claude/` dirs | Delete (Phase 9) | **Expected result**: complete inventory with disposition decisions. ### 0.5 Remove dead Claude Code plugin references **Precondition**: `~/.claude/settings.json` may reference CC-only plugins. ```bash cat ~/.claude/settings.json | python3 -c "import json,sys; d=json.load(sys.stdin); print(json.dumps(d.get('enabledPlugins',{}), indent=2))" ``` In ivp-book: removed 4 plugins (`glm-plan-usage`, `glm-plan-bug`, `infolead-claude-subscription-router`, `infolead-book-structure`) + marketplace entries + cache dirs + npm packages. **Postcondition**: `enabledPlugins` cleared to `{}`, `installed_plugins.json` cleared, `known_marketplaces.json` cleared, caches deleted. ### 0.6 Eliminate duplicate skills **Hypothesis**: same skill name may exist in both `~/.claude/skills/` and project `.claude/skills/`. ```bash for skill in .claude/skills/*/; do name=$(basename "$skill"); [ -d "$HOME/.claude/skills/$name" ] && echo "DUPLICATE: $name"; done ``` In ivp-book: `review-convergence` duplicated in both global and project — removed Claude Code copies, kept plugin's generic version. **Decision rule**: keep the version that is NOT Claude-Code-specific. If both are generic, keep project-local. If one is plugin-based, keep that. ### 0.7 Audit non-agent/skill `.md` files Review ALL `.md` files NOT under `.claude/agents/` or `.claude/skills/`: | Verdict | Condition | |---------|-----------| | KEEP | Referenced from active agents/skills/CLAUDE.md/opencode.json | | REMOVE | Duplicate of skill content, stale, or unreferenced | | REVIEW | Ambiguous — ask author | ```bash grep -r "filename.md" .claude/skills/ .claude/agents/ .claude/CLAUDE.md opencode.json 2>/dev/null ``` In ivp-book: `REVIEW_SYSTEM.md` and `mcp-integration.md` identified as unreferenced → deleted. **Expected result**: verdict table with decisions for every file. ### 0.8 Discover submodule `.opencode/` or `.claude/` agent dirs **Hypothesis**: submodules may have their own agent directories that opencode could scan. ```bash find . -path '*/.opencode/agents/*.md' -not -path './.opencode/*' | sort find . -path '*/.claude/agents/*.md' -not -path './.claude/*' | sort ``` In ivp-book: `literature/PIV-Formalization-ICSA2026/.opencode/agents/` had 15 agents with old `tools:` format — NOT converted (read-only submodule). **Decision**: do NOT touch submodule agents. If they cause boot errors, fix them in the submodule repo. ### 0.9 Check for `.opencode/` duplicates from prior migration attempts **Hypothesis**: a previous incomplete migration may have copied `.claude/agents/` to `.opencode/agents/`, creating duplicates with plugin agents. ```bash for f in .opencode/agents/*.md 2>/dev/null; do name=$(basename "$f" .md) plugin_path=$(find ../llm-harness-plugins/ -name "${name}.md" 2>/dev/null | head -1) [ -n "$plugin_path" ] && echo "DUPLICATE: $name — .opencode/ copy + plugin" done ``` **Decision**: keep the plugin copy; delete the `.opencode/agents/` duplicate. The `opencode.json` entry with `prompt: "{file:...}"` is the single source of truth. **Expected result**: zero duplicates remaining. --- ## Phase 1: Create `.agents/` infrastructure **Precondition**: Phase 0 complete. `.claude/project-context/` and `.claude/skills/` exist. **Postcondition**: `.agents/context/` and `.agents/skills/` populated with all migrated files. Original `.claude/` files untouched. **Gate**: `ls .agents/context/ .agents/skills/` shows expected file counts. ### 1.1 Move context files ```bash mkdir -p .agents/context cp .claude/project-context/* .agents/context/ 2>/dev/null for f in build-system.md preamble-setup.md latex-conventions.md writing-style.md \ typst-syntax-rules.md typst-migration-guide.md; do [ -f ".claude/$f" ] && cp ".claude/$f" ".agents/context/${f##*/}" done [ -f ".claude/systems/mcp-integration.md" ] && cp ".claude/systems/mcp-integration.md" .agents/context/ ``` ### 1.2 Move skills ```bash mkdir -p .agents/skills cp -r .claude/skills/* .agents/skills/ ``` --- ## Phase 2: `tools:` → `permission:` conversion **Precondition**: Phase 1 complete. `.opencode/agents/` populated with all agent `.md` files. **Postcondition**: Every agent `.md` file has a `permission:` block with all 10 keys. Zero `tools:` lines remain (except in code examples or README). **Failure mode**: OpenCode rejects comma-separated `tools:` on boot — `Configuration is invalid ... Expected object | undefined, got "Read, Glob, Grep"`. Fix: convert and reboot. **Gate**: `grep -rn "^tools:" .opencode/agents/*.md | grep -v README` returns empty. ### 2.1 Three `tools:` formats | Format | Example | Count (ivp-book) | |---|---|---| | **A** — comma-separated string | `tools: Read, Glob, Grep` | 110 | | **B** — YAML list | `tools:\n - Read\n - Write...` | 8 | | **C** — empty | `tools:` or `tools: ` | 9 | ### 2.2 Conversion rules **Format A → permission:** ```yaml # OLD tools: Read, Glob, Grep # NEW permission: read: allow glob: allow grep: allow edit: deny write: deny bash: deny task: deny webfetch: deny websearch: deny todowrite: deny ``` **Format B → permission:** ```yaml # OLD tools: - Read - Write - Edit - Bash - Grep - Glob # NEW permission: read: allow write: allow edit: allow bash: allow glob: allow grep: allow task: deny webfetch: deny websearch: deny todowrite: deny ``` **Format C** (empty `tools:`) — replace with all-deny block: ```yaml permission: read: deny glob: deny grep: deny edit: deny write: deny bash: deny task: deny webfetch: deny websearch: deny todowrite: deny ``` ### 2.3 Full `tools:` → `permission:` mapping All 10 keys must be present. Only keys listed in `tools:` get `allow`; rest get `deny`. | Original `tools:` value | `allow` keys | |---|---| | `Read, Glob, Grep` | read, glob, grep | | `Read, Glob, Grep, Bash` | + bash | | _(empty)_ | none (all deny) | | `Read, Glob, Grep, WebFetch` | + webfetch | | `Read, Edit, Glob, Grep` | + edit | | `Read, Glob, Grep, Write, Edit, Bash` | + write, edit, bash | | `Read, Write, Glob, Grep` | + write | | `Read, Write, Bash, Glob` | + write, bash | | `Read, Edit, Bash, Glob, Grep` | + edit, bash | | `Read, Write, Edit` | + write, edit | | `Read, Write, Bash` | + write, bash | | `Read, Edit, Write, Bash, Glob, Grep` | + write, edit, bash | | `Read, Edit, Glob, Grep, Bash` | + edit, bash | | `Read, Edit, Bash, ...` | + edit, bash | | `Read, Edit` | + edit | | YAML list format | each listed item | ### 2.4 Move agent files ```bash mkdir -p .opencode/agents cp -r .claude/agents/*.md .opencode/agents/ cp .claude/agents/README.md .opencode/agents/ ``` ### 2.5 Apply conversion ```bash grep -l "^tools:" .opencode/agents/*.md | grep -v README ``` **Do NOT touch**: README.md, files with existing `permission:` field, files with `tools:` object format. ### 2.6 Verification (pre-boot) After conversion, these MUST return zero: ```bash # Check 1: comma-string format grep -rn "^tools: Read," .opencode/agents/*.md | grep -v README && echo "FAIL" || echo "PASS" # Check 2: YAML list format (next line after "tools:" is "-") grep -rn -A1 "^tools:$\|^tools: $" .opencode/agents/*.md | grep "^- " && echo "FAIL" || echo "PASS" # Check 3: all files have permission block with all 10 keys for f in .opencode/agents/*.md; do [ "$f" = ".opencode/agents/README.md" ] && continue count=$(grep -cE "^\s+(read|glob|grep|edit|write|bash|task|webfetch|websearch|todowrite):" "$f") [ "$count" -ne 10 ] && echo "INCOMPLETE: $f ($count/10)" done ``` ### 2.7 Edge cases | Case | Example | Handling | |------|---------|----------| | `permissionMode: acceptEdits` coexisting with `tools:` | `typst-writer.md` | Keep alongside new `permission:` block | | Code-example `tools:` outside frontmatter | `markdown-agent.md:104` | Do NOT touch — only frontmatter (between first `---` pair) matters | | `model:` after `tools:` in frontmatter | `scenario-validity-fixer` | Preserve; YAML key order is irrelevant | --- ## Phase 3: Fix skill internal paths **Precondition**: Phase 2 complete. `.agents/skills/` populated. **Postcondition**: Zero `.claude/` path references remain in any skill file (except bare prohibitions like "never store in `.claude/`"). **Gate**: `grep -rn '\.claude/' .agents/skills/` returns only prohibition lines. ### 3.1 Path replacement table | Old path | New path | |---|---| | `.claude/project-context/agent-applicability.md` | `.agents/context/agent-applicability.md` | | `.claude/project-context/ivp-formulation.md` | `.agents/context/ivp-formulation.md` | | `.claude/project-context/ivp-classical-principles.md` | `.agents/context/ivp-classical-principles.md` | | `.claude/project-context/terminology.md` | `.agents/context/terminology.md` | | `.claude/project-context/skills.md` | `.agents/context/skills.md` | | `.claude/build-system.md` | `.agents/context/build-system.md` | | `.claude/preamble-setup.md` | `.agents/context/preamble-setup.md` | | `.claude/latex-conventions.md` | `.agents/context/latex-conventions.md` | | `.claude/writing-style.md` | `.agents/context/writing-style.md` | | `.claude/typst-syntax-rules.md` | `.agents/context/typst-syntax-rules.md` | | `.claude/typst-migration-guide.md` | `.agents/context/typst-migration-guide.md` | | `.claude/systems/mcp-integration.md` | `.agents/context/mcp-integration.md` | | `.claude/agents/<name>.md` | `.opencode/agents/<name>.md` | | `.claude/CLAUDE.md` | `AGENTS.md` | ### 3.2 Procedure ```bash grep -rn '\.claude/' .agents/skills/ # Fix each match. Use replaceAll for patterns that repeat within a file. # Do NOT change bare-prohibition lines ("never store in .claude/") — they describe a rule, not a path. ``` **Expected result**: in ivp-book, 29 files modified with 54 occurrences replaced. 7 prohibition lines left untouched in `verify-*` and `review-*` skills. --- ## Phase 4: Improve skill descriptions **Precondition**: Phase 3 complete. Skill files have valid frontmatter. **Postcondition**: Every skill description is ≤ 1024 chars, self-contained (what it does + when to use it), and uses imperative phrasing. **Gate**: `for f in .agents/skills/*/SKILL.md; do desc=$(sed -n '3s/^description: //p' "$f"); echo "${#desc} $f"; done` — all ≤ 1024. ### 4.1 Criteria (Agentskills spec) 1. Describe what the skill does 2. Describe when to use it 3. Use imperative phrasing 4. Be specific and keyword-rich 5. ≤ 1024 chars ### 4.2 Skills improved in ivp-book (11) | Skill | New description | |---|---| | `review-latex` | `Review legacy LaTeX content for errors before migration — checks citation accuracy, notation, logical errors, and style, then verifies the build with latexmk using the iterative fix-reverify pattern until zero findings. Use ONLY on legacy src/main/latex content; for new writing run review-typst instead.` | | `review-typst` | `Review Typst content for errors — runs Typst-specific checks (syntax, notation, citations, logical and style errors), verifies the build with typst compile, and repeats fix-reverify rounds until convergence to zero findings. Use on active src/main/typst content, the authoritative source for all new writing.` | | `review-chapter` | `Run a comprehensive chapter review that auto-detects LaTeX vs Typst source, executes five phases (structural, correctness, pedagogical, redundancy, style), and produces a consolidated report with severity breakdowns. Use on any single chapter before publication or when combining review agents into one pass.` | | `review-volume` | `Run a full volume review of every chapter plus volume-level cross-checks, auto-detecting LaTeX vs Typst format and warning about token budget limits. Use when you need a complete volume-wide quality gate before publication, not a single-chapter review.` | | `review-section` | `Run the full review pipeline for a single Typst section through mechanics, content, deep, and build stages, fixing findings in place and verifying the build. Use for section-level review where a chapter pass is too broad.` | | `review-redundancy` | `Review one or more chapters for repeated statements, duplicated arguments, and needless redundancy, iterating until convergence. Use to trim page count and remove duplicate arguments before publication or when chapters feel repetitive.` | | `review-full` | `Run the most expensive review operation for publication-readiness — launches volume review, exercise review, adversarial red-team, and companion document reviews in parallel. Use as the final gate before publishing.` | | `review-exercises` | `Read-only review focusing only on exercises and solutions — checks Bloom's alignment, solution correctness, coverage, and difficulty distribution, then reports findings without fixing. Use after content writing, when only exercise quality needs assessment.` | | `review-tikz` | `Review legacy LaTeX TikZ diagrams for layout issues (overlaps, margin violations, color and arrow visibility, legend placement, grayscale/B&W print safety). Read-only by default with optional fixing. Use only on legacy LaTeX diagrams, not Typst content.` | | `review-adversarial` | `Run all adversarial persona auditors (audience, rhetorical, structural, meta-theoretical attackers) against a chapter, producing a consolidated vulnerability report. Read-only — reports findings without applying fixes. Use to surface attack surfaces before the fix pipeline.` | | `structural-coherence` | `Assess how a chapter, section, subsection, or volume contributes to its parent unit — checking narrative arc, scope adherence, and derivation completeness across siblings. Complementary to review-chapter; use when structure itself, not content, needs scrutiny.` | ### 4.3 Verification ```bash for f in .agents/skills/*/SKILL.md; do desc=$(sed -n '3s/^description: //p' "$f") echo "${#desc} $f" done | sort -rn # All must be ≤ 1024 ``` --- ## Phase 5: Add `compatibility:` fields to skills **Precondition**: Phase 4 complete. Skill frontmatter has `name:` and `description:`. **Postcondition**: 13 skills have `compatibility:` field on line 4 (between `description:` and `---`). **Gate**: `grep -l '^compatibility:' .agents/skills/*/SKILL.md | wc -l` returns count of skills needing compatibility. ### 5.1 Frontmatter format ```yaml --- name: skill-name description: ... compatibility: Requires typst (typst compile) --- ``` ### 5.2 Template values (from ivp-book) | Skill | `compatibility:` value | |---|---| | `integrate-topic` | `Requires nix, typst, java (>=25), git, and internet access` | | `review-paper-publication` | `Requires typst and internet access for citation verification` | | `review-latex` | `Requires latexmk (TeX Live); applies to legacy LaTeX content only` | | `review-typst` | `Requires typst (typst compile)` | | `review-fix-tikz` | `Requires latexmk (TeX Live)` | | `review-fix-cetz` | `Requires typst (typst compile)` | | `review-fix-typst-diagrams` | `Requires typst (typst compile)` | | `verify-citations` | `Requires internet access for web search and source verification` | | `verify-empirical-claims` | `Requires internet access for web search and source verification` | | `verify-domain-applicability` | `Requires internet access for web search and domain-authority verification` | | `verify-novelty` | `Requires internet access for web search and prior-art comparison` | | `review-adversarial-deep` | `Requires internet access for live discourse search` | | `companion-raisondetre-review-fix` | `Requires typst (typst compile) and pdftotext for TOC comparison` | --- ## Phase 6: Rewrite `opencode.json` agent section **Precondition**: Phase 2 complete. `.opencode/agents/` has all agent `.md` files. Inventory from Phase 0 complete. **Postcondition**: Every agent with a `.opencode/agents/*.md` file has an entry in `opencode.json`. All plugin-only agents have `prompt:` fields. Zero orphan entries. JSON is valid. **Gate**: `python3 -c "import json; json.load(open('opencode.json')); print('valid')"` ### 6.1 Identify missing agents ```bash diff <(ls .opencode/agents/*.md | sed 's|.*/||; s|\.md||' | sort) \ <(python3 -c "import json; d=json.load(open('opencode.json')); print('\n'.join(sorted(d.get('agent',{}).keys())))") ``` In ivp-book, 4 agents had `.md` files but no `opencode.json` entry: `paper-drafter`, `paper-scaffolder`, `bibliography-auditor`, `config-auditor`. ### 6.2 Add `prompt:` fields for plugin-only agents Agents defined in plugins but NOT in `.opencode/agents/` must reference their `.md` body: ```json "bibliography-auditor": { "model": "deepseek/deepseek-v4-flash", "thinking": { "type": "enabled" }, "prompt": "{file:../llm-harness-plugins/general-skills/agents/bibliography-auditor.md}" } ``` Insert `"prompt"` as the **last property** in each agent block, after `"thinking": {...}`. Plugin-only agents from ivp-book (16): - general-skills: `bibliography-auditor`, `citation-fidelity-auditor`, `config-auditor`, `logic-auditor`, `math-verifier`, `proof-soundness-auditor`, `redundancy-auditor`, `style-auditor`, `style-naturalizer`, `xref-checker` - typst-toolkit: `typst-citation-checker`, `typst-diagram-checker`, `typst-formatting-fixer`, `typst-production-readiness-checker`, `typst-syntax-fixer`, `typst-xref-checker` ### 6.3 Agent model mapping CC tier → opencode model: - `haiku` → `deepseek/deepseek-v4-flash` - `sonnet` → `deepseek/deepseek-v4-pro` - `opus` → `deepseek/deepseek-v4-pro` (or `kimi-for-coding/k3` for proofs/diagrams) ### 6.4 Remove orphan agent entries ```bash python3 -c " import json, os d = json.load(open('opencode.json')) agents_dir = {os.path.splitext(f)[0] for f in os.listdir('.opencode/agents/') if f.endswith('.md')} opencode_agents = set(d.get('agent',{}).keys()) orphans = opencode_agents - agents_dir - {'memory-keeper', 'memory-dreamer'} for a in sorted(orphans): has_prompt = 'prompt' in d['agent'].get(a, {}) tag = 'KEEP (has prompt)' if has_prompt else 'REMOVE (orphan)' print(f'{a}: {tag}') " ``` **Decision**: KEEP if it has a `prompt:` field pointing to a valid plugin file. REMOVE otherwise. ### 6.5 Document deferred plugin agents These plugin agents exist but were NOT added to `opencode.json`: | Plugin | Agents | Reason | |---|---|---| | `latex-toolkit` | 9 agents | Legacy LaTeX only; project has decommissioned LaTeX | | `tier-router` | 4 agents | CC-specific routing; opencode uses different mechanism | --- ## Phase 7: Set agent mode — primary vs subagent **Precondition**: Phase 6 complete. `opencode.json` has all agent entries. **Postcondition**: `memory-keeper` and `memory-dreamer` have explicit `mode: subagent`. All others inherit default (subagent). No agent incorrectly set to `primary`. **Discovery**: `@memory-dreamer` worked in chat out of the box → investigation revealed the `agentmem` plugin spawns them at runtime → `opencode.json` `agent` block provides config → plugin `.md` provides prompt body. **Key insight**: `.md` frontmatter is NOT parsed from `{file:...}` references — only the body loads as prompt. Must restate model/mode/permissions in JSON. ### 7.1 Classification rules | Category | Mode | Reasoning | |---|---|---| | Auditors (~140) | `subagent` | Spawned by review skills; never user-facing | | Content generators | `subagent` | Spawned by authoring skills | | Language agents | `subagent` | Delegated for file operations | | Fixers | `subagent` | Spawned by review-fix loops | | Memory agents | `subagent` | Plugin-runtime spawns + `@mention` | | Infrastructure | `subagent` | Internal routing | | Built-in (`general`, `explore`) | N/A | Not configurable | **Rule**: almost all agents default to `mode: subagent`. `primary` is ONLY for agents the user launches directly via `opencode --agent <name>`. **Implementation**: do NOT add `mode` for agents that already default. Only add explicit `"mode": "subagent"` for agents with unusual fields (`memory-keeper`, `memory-dreamer` which have `prompt`, `permission`, `steps`). ### 7.2 Verification ```bash python3 -c " import json; d=json.load(open('opencode.json')) for name, cfg in d.get('agent',{}).items(): if 'mode' in cfg: print(f'{name}: mode={cfg[\"mode\"]}') " # Expected: only memory-keeper and memory-dreamer ``` --- ## Phase 8: Convert CLAUDE.md → AGENTS.md **Precondition**: Phase 6 complete. `.claude/CLAUDE.md` exists. **Postcondition**: `AGENTS.md` at project root with all path references updated, model escalation removed, agent system section rewritten for opencode. **Gate**: `grep '\.claude/' AGENTS.md` returns zero loadable-path references. ### 8.1 Structural edits | Edit | Old → New | |---|---| | Title | `# CLAUDE.md` → `# AGENTS.md` | | Agent heading | `## Subagent System` → `## Agent System` | | Agent description | Replace "Custom subagents in `.claude/agents/`..." with "Agent definitions: see `opencode.json` `agent` section..." | | Model escalation | Delete all lines (e.g., "Model escalation: glm-4.7...") | | Model-tier labels | `agent-name (Sonnet)` → `agent-name` | | Skills path | `.claude/project-context/skills.md` → `.agents/context/skills.md` | | Context paths | All `.claude/` → `.agents/context/` | | Agent refs | `.claude/agents/` → `.opencode/agents/` | | Global CLAUDE.md refs | `~/.claude/CLAUDE.md` → "global config files" | ### 8.2 Update `opencode.json` instructions ```json "Read .claude/agents/*.md for agent definitions." → "Read .opencode/agents/*.md for agent definitions." ``` Add tier instructions if missing: ```json "3 tiers: deepseek-v4-flash (mechanical), deepseek-v4-pro (complex + deep + code). kimi-for-coding/k3 reserved for vision-enabled tasks (diagram/layout) and deep-reasoning (logic/proof)." ``` ### 8.3 OpenCode compatibility review 1. Agent invocation → OpenCode uses Task tool with `subagent_type`, not CC's auto-delegation 2. Skills → same slash-command model in both 3. No remaining `.claude/` paths 4. No model escalation logic --- ## Phase 9: Commit in logical order **Precondition**: Phases 1-8 complete. All files modified, `.claude/` still present. **Postcondition**: 3 commits in git. `.claude/` deleted. `.agentmem/` in `.gitignore`. **Gate**: `git status` shows clean working tree. ### 9.1 Commit 1: Agent configs (tools→permission) ```bash git add .opencode/agents/ git commit -m "fix(agents): migrate tools field to permission format for opencode compatibility" ``` **Expected**: 169 new files in `.opencode/agents/`. ### 9.2 Commit 2: Full migration ```bash git add .agents/ git add AGENTS.md git add opencode.json git rm -r .claude/ 2>/dev/null git rm .claudelens 2>/dev/null find . -path '*/.claude' -not -path './.claude' -exec git rm -r {} \; 2>/dev/null git commit -m "chore: migrate from Claude Code to OpenCode — agents, skills, config, context" ``` **Expected**: `.claude/` deleted (~250 deleted files in ivp-book), `.agents/` and `AGENTS.md` added, `opencode.json` modified. ### 9.3 Commit 3: Mode assignments + gitignore ```bash echo '.agentmem/' >> .gitignore git add .gitignore # git add opencode.json # if mode changes were applied git commit -m "config: convert all project agents to subagent-only mode" ``` **Decision**: `.agentmem/` is local session state, NOT source of truth. The user asked explicitly: "should agent memory go into git, or is MEMORY something eminently local?" Answer: it is local. Also verify `tmp/` is in `.gitignore`. --- ## Phase 10: Post-migration verification **Precondition**: Phase 9 complete. All commits made. **Postcondition**: opencode boots cleanly. Zero `tools:` errors. Zero `.claude/` path references. Agent counts reconcile. **Gate**: opencode launches on port 34033 without `Configuration is invalid` errors. ### 10.1 Path grepping ```bash grep -r '\.claude/' .agents/ .opencode/ AGENTS.md opencode.json 2>/dev/null # Expected: zero (except comments/examples) ``` ### 10.2 Agent count reconciliation ```bash echo "Agent .md files: $(ls .opencode/agents/*.md 2>/dev/null | grep -v README | wc -l)" python3 -c "import json; d=json.load(open('opencode.json')); print(f'opencode.json entries: {len(d.get(\"agent\",{}))}')" ``` In ivp-book: 168 `.md` + 2 JSON-only (`memory-keeper`, `memory-dreamer`) + 16 plugin `prompt` entries = 186. ### 10.3 Iterative boot-test-fix **This was the most error-prone step.** OpenCode validates all configs on boot; the first invalid agent blocks startup: ``` boot → fails on agent N → fix N → boot → fails on N+1 → ... ``` **Proactive verification (run BEFORE booting):** ```bash # Check 1: comma-string format grep -rn "^tools: Read," .opencode/agents/*.md | grep -v README && echo "FAIL" || echo "PASS" # Check 2: YAML list format grep -rn -A1 "^tools:$\|^tools: $" .opencode/agents/*.md | grep "^- " && echo "FAIL" || echo "PASS" # Check 3: permission block in every file (10 keys) for f in .opencode/agents/*.md; do [ "$f" = ".opencode/agents/README.md" ] && continue count=$(grep -cE "^\s+(read|glob|grep|edit|write|bash|task|webfetch|websearch|todowrite):" "$f") [ "$count" -ne 10 ] && echo "INCOMPLETE ($count/10): $f" done # Then boot: opencode --port 34033 & sleep 2 ``` **Error → fix → reboot** pattern: | Error message | Cause | Fix | |---|---|---| | `Expected object \| undefined, got "Read, Glob, Grep"` | Comma-string `tools:` | Convert to `permission:` block | | `Expected object \| undefined, got [...]` | YAML list `tools:` | Convert to `permission:` block | | `failed to load plugin` | Pre-existing plugin issue | NOT migration-related — audit separately | | `duplicate skill name` | Skill in two locations | Delete old copy from `~/.claude/skills/` or `~/.opencode/skills/` | ### 10.4 Permission completeness check ```bash for f in .opencode/agents/*.md; do count=$(grep -cE "^\s+(read|glob|grep|edit|write|bash|task|webfetch|websearch|todowrite):" "$f" 2>/dev/null) [ "$count" -lt 10 ] && echo "INCOMPLETE: $f ($count/10)" done # Expected: zero results ``` --- ## Things deliberately NOT migrated | Artifact | Reason | |---|---| | `.claudelens` | Budget/ROI — no opencode equivalent | | `.claude/settings.json` | CC permission system — replaced by agent-level `permission:` | | `.claude/hooks/` | Java hooks — no direct equivalent; port to plugins or drop | | `.claude/memory/` + `.claude/projects/` | Session/project memory — key learnings extracted to AGENTS.md; rest discarded | | `.claude/REVIEW_SYSTEM.md` | Duplicate of skills — skills are self-documenting | | `.claude/plugins/` | CC plugin cache — irrelevant after migration | | Subdir `.claude/` remnants | Any `.claude/` outside root (e.g., `src/main/latex/volume-1/.claude/`) | | `.agentmem/MEMORY.md` | Local session state, NOT source of truth — gitignored | | Duplicate CC agent `.md` files | Plugin copy + opencode.json entry is the single source of truth |