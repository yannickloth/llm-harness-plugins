{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "CONTENT=$(cat | python3 -c \"import sys,json; ti=json.load(sys.stdin); out=ti.get('tool_output',''); print(out[:50000])\" 2>/dev/null); echo \"$CONTENT\" | \"${CLAUDE_PLUGIN_ROOT}\"/bin/gcl scan-secrets --"
          }
        ]
      }
    ]
  }
}
